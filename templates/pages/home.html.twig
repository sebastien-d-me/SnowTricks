{% extends "base.html.twig" %}
{% set pageId = "home" %}


{% block main %}
    <div class="home__banner">
        <img alt="Banner of the website" class="banner__image" loading="lazy" src="{{ asset('assets/images/home/banner.webp') }}">
        <span class="banner__text">Lorem, ipsum dolor.</span>
        <a class="banner__arrow link" href="#tricks__list">
            <i class="ri-arrow-down-line"></i>
        </a>
    </div>

    <div id="tricks__list">
        {% for tricks in tricksData %}
            <div class="tricks__item">
                <img alt="Illustration of a {{ tricks.trick.name }}" class="trick__image" loading="lazy" src="assets/{{ tricks.cover.path }}">
                <div class="trick__informations">
                    <a class="link link--secondary" href="/trick/{{ tricks.trick.id }}">
                        <span class="trick__name">{{ tricks.trick.name }}</span>
                    </a>
                    <div class="trick__action">
                        <a class="link link--icon" href="/trick/edit/{{ tricks.trick.id }}">
                            <i class="ri-pencil-line"></i>
                        </a>
                        <a class="link link--icon" href="/trick/delete/{{ tricks.trick.id }}">
                            <i class="ri-delete-bin-line"></i>
                        </a>
                    </div>
                </div>
            </div>
        {% endfor %}

        {% if tricksData is empty %}
            <span>Aucun trick n'a été écrit pour le moment...</span>
        {% elseif tricksData | length > 15 %}
            <div class="tricks__break"></div>
            <button class="btn btn__tricks">
                <i class="ri-refresh-line"></i>
                <span>Charger plus</span>
            </button>
        {% else %}
            <div class="tricks__break"></div>
        {% endif %}        
    </div>

    <a class="tricks__arrow link" href="#tricks__list">
        <i class="ri-arrow-up-line"></i>
    </a>
{% endblock %}